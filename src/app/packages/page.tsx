import PackagesMain from '@/pages/packages/packages-main';
import React from 'react';

const PackagesPage = () => {
    return (
        <div>
            <PackagesMain />
        </div>
    );
};

export default PackagesPage;